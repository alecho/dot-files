# Environment

**Summary**: Local setup, environment variables, and secrets for {{PROJECT_NAME}}.

**Sources**: (none yet — fill in as the page grows)

**Last updated**: {{DATE}}

---

Stub. Fill in:

- Required runtimes / tool versions (and where they're pinned, e.g. `mise.toml`)
- Required environment variables and where to source them
- Per-worktree overrides (e.g. `mise.local.toml` patterns)
- Secrets handling

## Related pages

- [[index]]
