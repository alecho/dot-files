return {
  {
    "folke/which-key.nvim",
    event = "VeryLazy",
    config = function()
      -- Basic configuration is already in the existing which-key.lua file
      -- This file is just to ensure proper loading with lazy.nvim
      
      -- You can add any additional which-key specific configuration here if needed
    end,
  }
}
